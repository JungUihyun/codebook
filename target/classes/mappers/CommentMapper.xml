<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 댓글 -->
<mapper namespace="net.gondr.mappers.CommentMapper">
	<insert id="comment_write">
		INSERT INTO spring_comments (content, writer, board_idx)
		VALUES ( #{content}, #{writer}, #{board_idx} )
	</insert>
	<select id="comment_list" resultType="CommentVO">
		SELECT idx, title, writer, board_idx FROM spring_comments WHERE board_idx = #{board_idx}
	</select>
	<delete id="comment_delete">
		DELETE FROM spring_comments WHERE idx = #{idx}
	</delete>
	<select id="comment_cnt" resultType="Integer">
		SELECT count(*) FROM spring_comments WHERE board_idx = #{board_idx}
	</select>
</mapper>